{% extends 'base.html.twig' %}

{% block body %}
    <div class="container mt-5 mb-5">
        <div class="row">
            <div class="col">
                <div class="login-wrap">
                    <p>Si un compte correspondant à votre adresse e-mail existe, un e-mail contenant un lien que vous pouvez utiliser pour réinitialiser votre mot de passe vient d'être envoyé.<br> 
                    <div class="text-danger">Ce lien expirera dans {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }} heure.</div>
                    </p>
                    <p>Si vous n'avez pas reçu d'e-mail, veuillez vérifier votre dossier spam ou <a href="{{ path('app_forgot_password_request') }}">Réessayez</a>.</p>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
